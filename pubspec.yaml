name: forenza_mobile
description: "FORENZA — Forensic Evidence Chain of Custody & Tamper-Evident Mobile Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.2
  supabase_flutter: ^2.5.6
  crypto: ^3.0.3
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1
  geolocator: ^11.0.0
  camera: ^0.10.5+9
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  http: ^1.2.1
  lucide_icons: ^0.257.0
  google_fonts: ^6.2.1
  flutter_dotenv: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - .env
